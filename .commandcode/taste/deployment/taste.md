# Deployment
- For the photo-sorter deployment, user prefers using Portainer to manage containers and GitHub as the code transfer method rather than scp/direct copy. Confidence: 0.7
- For remote NAS/network bridge workflows (WSL2 + WireGuard + CIFS), user prefers deploying directly to the home server (192.168.0.31) instead of pushing the stack through WSL2. Confidence: 0.60
- For the photo-sorter deployment, the server's photos mount path is /srv/mergerfs/pool/pool/photos/Unsorted (container /photos), with Best exported at /srv/mergerfs/pool/pool/photos/Best (not /mnt/nas/pool/photos). Confidence: 0.80
- For the photo-sorter production deployment, the app should bind to port 8420, not 8080 (8080 is unavailable on the host). Confidence: 0.60
- For the photo-sorter Portainer stack deployment, rename the env file to stack.env (Portainer convention) rather than .env (dev naming). Confidence: 0.70
- For the photo-sorter Portainer deployment, the container image is built locally (tagged photo-sorter:local from the repo's Dockerfile), not pulled from a registry; the Portainer "Build" option must be checked or redeploy fails with "pull access denied". Confidence: 0.70
- For photo-sorter, Portainer deploys `docker-compose.yml` by default rather than `compose.prod.yml`, so `docker-compose.yml` must have working defaults (e.g., `${PHOTOS_DIR:-/srv/mergerfs/pool/pool/photos/Unsorted}` and `${BEST_DIR:-/srv/.../Best}`) so empty env vars don't collapse the /photos and /export mounts. Confidence: 0.65
- For the photo-sorter container, the compose service should set `user: "1000:100"` to run as the NAS UID/GID (and the Dockerfile should create/chown /photos and /export dirs) so the app can read/write the mergerfs photo mount. This resolved the inability to access /photos. Confidence: 0.70
- The Dockerfile's non-root user must be created with the same UID:GID as the compose runtime user (`user: "1000:100"`), not a random system user — otherwise a freshly-created /data volume is owned by the wrong UID and the app crashes at startup with `PermissionError: [Errno 13] Permission denied: '/data/thumbnails'`, needing a manual `docker run --rm ... chown -R 1000:100 /data` workaround. The user asked for this root cause to be fixed in the Dockerfile ("Good, now also fix the docker") so future volume recreation doesn't need manual chown, rather than only patching the live volume. Confidence: 0.8
- User expects the assistant to actively verify deployed app state after each fix (curl health/scan/photos/SSE endpoints through the tunnel) instead of trusting the redeploy, and triggers re-verification with terse imperative commands like "Test now" or "Verify pipeline". The user also checks on the app's live state with terse questions like "What is the app doing now?", expecting a concise status summary (progress per stage, whether anything is running, next step) drawn from live /api/progress + /api/health rather than a guess. The user also announces completed deployment steps in single terse words (e.g., "Redeployed") and expects the assistant to immediately re-verify the fix and re-run the affected operation — e.g., reporting "Re-deployed. Photos are still 256" right after a redeploy, expecting the assistant to re-scan, re-check, and diagnose rather than explain the redeploy. Confidence: 0.99 The user also independently checks server logs to confirm whether operations actually started (e.g., "I don't see scan starting in the logs:") and proactively volunteers their own diagnostic hypotheses in terse statements (e.g., "I think whole server hung up", "Other services on the server are unreachable."). Confidence: 0.9
- User prefers the photo-sorter repo to keep only `compose.prod.yml` + `Dockerfile` for deployment; the dev `docker-compose.yml` was deliberately removed (via the user's own "Fix fix" commits) so the repo is Portainer-only with a single compose file, and the assistant should preserve that structure when editing. Confidence: 0.6
- For the photo-sorter deployment, the Portainer stack is named `ps` (container `ps-photo-sorter-1`, named volume `ps_photo-data` mounted at /data). Confidence: 0.7
- When the container's user was switched to `1000:100`, the pre-existing `ps_photo-data` volume (created by the earlier appuser image, UID ~999) still blocked the app — DB writes, and even creating /data/thumbnails at startup (crashes with `PermissionError: [Errno 13] Permission denied: '/data/thumbnails'`); the one-time fix is `docker run --rm -v ps_photo-data:/data alpine chown -R 1000:100 /data` followed by a container restart. Confidence: 0.85
- User runs diagnostic docker commands on the home server (e.g., `docker inspect --format '{{json .Mounts}}' ps-photo-sorter-1`) and pastes the raw output for the assistant to interpret, since the assistant's shell only reaches the local Windows machine. This extends to full server/container error tracebacks (e.g., pasted a complete sqlalchemy/sqlite startup-failure stack trace) — the user reports problems by pasting verbatim logs or exact on-screen UI text (e.g., the Review tab's message "No groups yet — run Scan + Analyze + Group on the Home tab.") rather than summarizing symptoms — e.g., starting a message with "Container logs:" or "sqlite3: command not found" to hand the raw output to the assistant for interpretation — including reporting the exact result of commands the assistant handed off (e.g., "Deleted 1554 duplicate rows" as the entire message) — including pasting a full container log mid-session that exposed a "Background job failed" FK traceback the UI had masked, and reporting the exact result of a handed-off backfill command as the entire message ("updated sha1 for 8752 jpgs"). Confidence: 0.99
- The photo-sorter container image is slim and does NOT include the sqlite3 CLI; DB diagnostics/edits on the server must use Python's built-in sqlite3 module instead (e.g., `python -c "import sqlite3; ..."` from the container console), not the `sqlite3` command. Confidence: 0.8
- User prefers that long-running operations (e.g., photo scans) run as background jobs that return immediately and stream progress via SSE, so a slow/hung NAS or mergerfs pool can never block the API or starve health checks. User also expects that progress to actually reach the UI — complaining "the interface tells me nothing on progress" when a re-scan's progress isn't visibly surfaced. Confidence: 0.7
- The home server's Docker host has only a small 128 GB disk; user is space-conscious, so deployments should keep image/volume footprints lean and disk usage should be considered/checked (e.g., `docker system df`, volume `du`) when diagnosing issues or planning features. Confidence: 0.7
- User is wary of destructive operations on production data (redeploys, volume deletion, DB row deletes) — they ask terse questions first ("the progress will be lost?", "Should I delete the /data volume of the container too?") and expect the assistant to confirm what persists vs. resets (e.g., catalog/DB rows and thumbnails survive in the /data volume) and to prefer surgical, targeted fixes (e.g., deleting only the duplicate catalogue-2 rows) over broad volume deletion. However, when data integrity itself is in doubt (e.g., duplicate photo rows from a second catalogue scan showing "multiple of the same photo in each group"), the user proactively proposes a full database clear ("Can we clear the database?") for a clean rebuild rather than more surgery. Confidence: 0.6

- User expects completed fixes to be committed AND pushed to the GitHub remote, and checks that the remote is in sync with terse questions like "Did you update github?" — the assistant should verify/confirm push state (git log, status, remote) when wrapping up a change so the Portainer pull-and-redeploy picks up the latest code. Confidence: 0.7
- When an already-deployed fix still doesn't take effect in production (e.g., jobs still not running after a redeploy), the assistant's next step is to suspect and test a stale/cached image — verify the deployed container actually carries the latest code (e.g., the backend commit behind the current frontend hash), and ask the user to force a clean rebuild/recreate in Portainer (e.g., `docker rmi photo-sorter:local` on the server, then redeploy so the image must rebuild from the Dockerfile). Proof of staleness can come from comparing the traceback line numbers in the deployed container's error against the fixed repo (old line numbers = old code). Confidence: 0.7
- To prove which code a deployed container runs, the assistant hands off a targeted `grep` for a distinctive code marker (e.g., `docker exec ps-photo-sorter-1 grep -n "THUMB_SIZE" /app/app/scanner.py`), the user runs it and pastes the raw output verbatim, and the assistant uses the hit/miss (line number + exact expression) to decide between "stale image" vs. "logic bug in the deployed code" before further digging. Confidence: 0.8
eate in Portainer. Confidence: 0.5
- When verifying whether the deployed photo-sorter image is current, the user's Docker host/Portainer UI does not show a CREATED column in `docker images` output (user reported: 'No "CREATED" column'); the assistant should hand off `docker images photo-sorter:local --format "{{.Tag}} {{.ID}} {{.CreatedAt}}"` (plus a `docker exec ps-photo-sorter-1 grep -n "THUMB_SIZE" /app/app/scanner.py` to prove which code the running container actually carries) rather than expecting default-column output. The user runs the exact handed-off command and pastes the raw output verbatim as the entire reply (e.g., "local eefe72b77a93 2026-08-06 00:15:23 +0200 CEST"; later pasting only the grep line "101:        sha1 = thumbnail_sha1(path, size, mtime, config.THUMB_SIZE)" with no commentary, leaving interpretation to the assistant — including pasting the full `docker exec` python diagnostic output "THUMB_SIZE: 512 / stored sha1 would be: 3053b022..." to let the assistant decide between stale config vs. logic bug). Confidence: 0.9
